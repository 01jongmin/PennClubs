# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'PennClubs' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SDWebImage', '~> 5.0'

  # Pods for PennClubs

  target 'PennClubsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PennClubsUITests' do
    # Pods for testing
  end

end
